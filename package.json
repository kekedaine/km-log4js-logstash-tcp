{
  "main": "./index.js",
  "name": "km-log4js-logstash-tcp",
  "scripts": {
    "test": "nodeunit tests"
  },
  "version": "1.0.2",
  "author": "dainv",
  "license": "ISC",
  "description": "This is a copy of the logstashUDP appender but instead sending via UDP send via TCP to avoid the maximum 64k bytes message size with the logstashUDP appender.",
  "devDependencies": {
    "log4js": "^2.4.1",
    "nodeunit": "^0.11.2"
  },
  "engines": {
    "node": ">= 6.0.0"
  },
  "keywords": [
    "logstash",
    "log4js",
    "appender"
  ],
  "repository": "dainv/km-log4js-logstash-tcp"
}
